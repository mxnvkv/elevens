import { AccountSettingsKeyPipe } from './account-settings-key.pipe';

describe('AccountSettingsKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountSettingsKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
